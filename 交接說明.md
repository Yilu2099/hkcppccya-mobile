# 換機繼續開發 — 交接說明

## 1. 需要的環境
- macOS，Python 3（系統自帶即可）；首次打包會自動安裝 `zhconv`（簡繁字表），需要能連網
- `git`；如要同步到 GitHub 需先 `gh auth login`

## 2. 目錄
- `src/index.template.html` — 唯一要改的源檔（樣式、頁面、資料）
- `assets/` — 圖片（base64 JSON）、消息、名單等資料
- `build.py` — 打包：輸出 `index.html`（Artifact 用）、`preview.html`（離線單檔）、`dist/`（線上版，圖片外置）
- `deploy.sh` — 打包並 scp 上線

## 3. 上線需要的 SSH（不在壓縮包內，請另外複製）
`deploy.sh` 用別名 `t2099-collector` 連服務器。請把舊 Mac 上這兩個文件複製到新機同樣位置：
- `~/.ssh/config` 裡的這段：
```
Host t2099-collector
  HostName 47.121.24.233
  User root
  IdentityFile ~/.ssh/id_ed25519_t2099
  IdentitiesOnly yes
  StrictHostKeyChecking accept-new
```
- 私鑰 `~/.ssh/id_ed25519_t2099`（複製後 `chmod 600`）

## 4. 日常流程
```bash
python3 build.py      # 本地打包，雙擊 preview.html 預覽
./deploy.sh           # 上線
git add -A && git commit -m "..." && git push   # 備份到 GitHub
```
